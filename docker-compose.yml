version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: resume_mysql_db
    environment:
      MYSQL_DATABASE: resumereviewdb
      MYSQL_ROOT_PASSWORD: secretpassword
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: